import 'package:flutter/material.dart';

/// Runs a shell command in a project folder on the PC and shows its output.
Future<void> showRunCommandSheet(
  BuildContext context,
  String projectId,
) async {}
