/// The app version shown in Settings > About. It mirrors the `version:` line
/// in pubspec.yaml; change both with `tool/bump_version.dart`, never by hand.
/// See VERSIONING.md.
library;

const appVersion = '2.0.0';
const appBuildNumber = 10;
