name: ai_dashboard_server
description: "Runs on the main PC: drives the agent CLIs and serves the dashboard's API and web app."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.13.4

dependencies:
  agent_core:
    path: ../packages/agent_core
  args: ^2.7.0
  path: ^1.9.1
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  http: ^1.6.0
  lints: ^6.1.0
  test: ^1.31.1
