name: agent_core
description: "Models, backend contract, mock backend and wire protocol shared by the app and the PC server."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.13.4

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
